<Window x:Class="ObserverExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ObserverExample"
        mc:Ignorable="d"
        Title="Stock Price Monitor" WindowStartupLocation="CenterScreen" Height="450" Width="800">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBox x:Name="StockSymbolTextBox" Text="{Binding Symbol, Mode=TwoWay}" Width="100" Margin="5"/>
                <Button Content="Subscribe" Click="Subscribe_Click" Margin="5" Padding="10,0"/>
                <Button Content="Unsubscribe" Click="Unsubscribe_Click" Margin="5" Padding="10,0"/>
                <Button Content="Start Updates" Click="StartUpdates_Click" Margin="5" Padding="10,0"/>
                <Button Content="Stop Updates" Click="StopUpdates_Click" Margin="5" Padding="10,0"/>
            </StackPanel>

            <DataGrid x:Name="StockDataGrid" 
                      ItemsSource="{Binding Path=Stocks}" 
                      Grid.Row="1" Margin="10" 
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="100"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="100"/>
                    <DataGridTextColumn Header="Change" Binding="{Binding Change}" Width="100"/>
                    <DataGridTextColumn Header="Last Updated" Binding="{Binding Timestamp}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Grid.Row="1" x:Name="StatusText" Text="{Binding StatusMessage, Mode=OneWay}" VerticalAlignment="Bottom" 
                   HorizontalAlignment="Right" Margin="10" Foreground="Gray"/>
        </Grid>
    </Window>
